defmodule EctoCSV.Table do

  @doc """
  Fetches all the data assocaited with a schema.
  It takes in a `schema` and a `loader`
  """
  def fetch_data(schema, loader)
  def fetch_data(schema, :eager) do

  end
  def fetch_data(schema, :lazy) do

  end
end
